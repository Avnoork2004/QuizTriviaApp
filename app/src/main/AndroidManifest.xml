<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizTriviaApp">
        <activity
            android:name=".ui2.SplashActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.QuizTriviaApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Other activities -->
        <activity android:name=".ui2.AuthActivity" />
        <activity android:name=".ui2.RegisterActivity" />
        <activity android:name=".ui2.RulesActivity" />
        <activity android:name=".ui2.QuizActivity" />
        <activity android:name=".ui2.ResultActivity" />
        <activity android:name=".ui2.HistoryActivity" />

        <!-- Optional: keep MainActivity if needed -->
        <activity android:name=".MainActivity" />

    </application>

</manifest>